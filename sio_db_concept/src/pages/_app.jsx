import "../styles/globals.css"; // Assuming your global CSS file is in the 'styles' directory

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
